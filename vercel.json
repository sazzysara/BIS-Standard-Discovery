{
  "buildCommand": "npm install --prefix frontend",
  "outputDirectory": "frontend/build",
  "framework": "react",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
